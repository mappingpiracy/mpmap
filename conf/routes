# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Mapdata index method -- doesn't really return anything yet.
GET     /mapdata                    controllers.MapData.index()
POST    /mapdata/events             controllers.MapData.events()
GET     /mapdata/events             controllers.MapData.events()
GET     /mapdata/export             controllers.MapData.export()
POST    /mapdata/export             controllers.MapData.export()

GET     /mybatistest                @controllers.Application.mybatistest()

# Static pages
GET     /about                      controllers.Application.about()
GET     /help                       controllers.Application.help()


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
